# Trust Memo — {{PROJECT_NAME}}

What's reliable, what's noisy, what's unassessable. Cites `F-NNN` ids from `analysis/output/findings.json`.

## Reliable

{{FIRST_ENTRY — list claims you stand behind. Each item should cite ≥1 finding id.}}

Example (replace with real findings — use literal `F-NNN` in examples; real entries use digits):
- Median session rating is 4.2 across 312 sessions [`F-NNN`].
- Quote provenance enforced for all verbatim citations [`F-NNN`].

## Noisy

What we'd report with caveats, not headline.

Example:
- Variance in coach-level effects (n per coach is small) [`F-NNN`].

## Unassessable

What we cannot conclude from the available data.

Example:
- Long-term outcomes — no follow-up data captured beyond session 8.

## Limitations not yet resolved

Open questions that would change recommendations if answered.

## Last updated

{{CREATED_AT}}
